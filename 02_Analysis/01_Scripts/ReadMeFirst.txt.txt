Note:

Change the amouunt of CPU's you have into ipynb-files and into zarr_processing.py*

*zarr_processing.py has funnctions linked into parallellized processing and chunks. Sices in these must be based on your data size. 